apiVersion: v1
kind: Service
metadata:
  name: {{ include "xnxgames_charts.fullname" . }}
  labels:
    {{- include "xnxgames_charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xnxgames_charts.selectorLabels" . | nindent 4 }}
